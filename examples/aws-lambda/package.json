{
  "name": "testimonials-lambda",
  "version": "1.0.0",
  "main": "handler.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r function.zip handler.js node_modules"
  },
  "dependencies": {
    "@types/aws-lambda": "^8.10.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}